monorepo:
  expects:
    - foo1
  produces:
    - bar1
    - bar2
  matches: bar/no-match
  excluded_steps:
    - label: "Some other step"
      command: "echo 'bar was replaced'"

env:
  BAR: some-value

steps:
  - command: echo "bar1"
  - command: echo "bar2"
