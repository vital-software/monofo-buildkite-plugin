monorepo:
  pure: true
  matches:
    - plugin.yml
    - "**/*.md"
    - .buildkite/plugin-lint

steps:
  - name: ":buildkite: Plugin Lint"
    key: plugin-lint
    timeout_in_minutes: 20
    plugins:
      - docker-compose#v3.9.0:
          run: plugin-lint
