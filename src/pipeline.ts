import { Config } from './config';
import { DiffMatchResult } from './diff';

export async function getPipeline(config: Config[], diff: DiffMatchResult): Promise<void> {
  return;
}

export async function outputPipeline(): Promise<void> {
  return;
}
